var VarControlError=0;
function Error(){
	var DivError= $('button')find('GenerarError');

	if(VarControlError==0){
		$(DivError).attr('disabled');
	}else{
		$('input').create("hello")
	}
	
}

$('button').click(function GenError(){
	$('button').attr('disabled',true);
});